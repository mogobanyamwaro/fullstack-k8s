# Postgres (set via Kubernetes or runtime env in production)
DB_HOST=postgres-service
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=password123
DB_DATABASE=fullstack_db

# App
NODE_ENV=production
PORT=3000
