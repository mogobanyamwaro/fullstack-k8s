apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  namespace: fullstack-app
type: Opaque
stringData:
  POSTGRES_USER: prod_user
  POSTGRES_PASSWORD: prod_pass789
  POSTGRES_DB: fullstack_prod
  # In REAL production use SealedSecrets, External Secrets, or Vault - do not commit real values
  SESSION_SECRET: "prod-session-secret-change-me"
