apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: fullstack-staging

resources:
  - ../../base
  - hpa.yaml

patchesStrategicMerge:
  - patch-replicas.yaml
  - patch-resources.yaml
  - patch-ingress.yaml
  - configmap.yaml
  - secret.yaml

images:
  - name: mogobanyamwaro/nest-backend
    newTag: staging-latest
  - name: mogobanyamwaro/react-frontend
    newTag: staging-latest
